// import {
// } from './homeActions';

const initialState = {
};

export default function loginScene(state = initialState, action) {
  switch (action.type) {
    default:
      // nothing to do
      return state;
  }
}
