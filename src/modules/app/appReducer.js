// action types
// import {
// } from './appActions';

const initialState = {
};

export default function app(state = initialState, action) {
  switch (action.type) {
    default: {
      // nothing to do
      return state;
    }
  }
}
